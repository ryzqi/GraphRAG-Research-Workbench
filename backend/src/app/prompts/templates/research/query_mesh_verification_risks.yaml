version: "1.0"
name: research_query_mesh_verification_risks
description: "Deep Research query mesh verification risks query"

template: |
  {canonical_query} 限制 风险 争议 反例

variables:
  - name: canonical_query
    type: string
    required: true
