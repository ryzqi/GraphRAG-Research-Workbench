version: "1.0"
name: research_query_mesh_verification_crosscheck
description: "Deep Research query mesh verification crosscheck query"

template: |
  {canonical_query} 官方资料 一手资料 交叉验证

variables:
  - name: canonical_query
    type: string
    required: true
